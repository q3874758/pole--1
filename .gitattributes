# Release-manifest signature fixtures are byte-exact: the cosign
# signature and certificate cover the exact committed bytes, so
# Windows checkouts must NOT convert their line endings.
tests/fixtures/** -text
